/**
 * @file This stores our general configuration.
 * @author Joshua Goodman
 */

// We define the configuration.
const config = {
  debug: true,
  domain: "localhost",
  port: 3560,
  rowsPerPage: 3,
  scheme: "http"
};



// We export the configuration.
module.exports = config;